<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="fishing.tackle.shop.ui.role.RoleActivity"
        android:label="@string/menu_stapovi"
        tools:layout="@layout/activity_role" />

    <fragment
        android:id="@+id/nav_gallery"
        android:name="fishing.tackle.shop.ui.stapovi.StapoviActivity"
        android:label="@string/menu_role"
        tools:layout="@layout/activity_stapovi" />

    <fragment
        android:id="@+id/nav_slideshow"
        android:name="fishing.tackle.shop.ui.satori.SatoriFragment"
        android:label="@string/menu_satori"
        tools:layout="@layout/activity_satori" />
</navigation>